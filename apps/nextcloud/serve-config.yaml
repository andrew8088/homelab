apiVersion: v1
kind: ConfigMap
metadata:
  name: tailscale-serve-config
  namespace: nextcloud
data:
  serve.json: |
    {
      "TCP": {
        "443": {
          "HTTPS": true
        }
      },
      "Web": {
        "nextcloud.twin-barley.ts.net:443": {
          "Handlers": {
            "/": {
              "Proxy": "http://127.0.0.1:80"
            }
          }
        }
      }
    }
